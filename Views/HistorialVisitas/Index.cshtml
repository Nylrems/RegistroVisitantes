@model IEnumerable<RegistroDeVisitantes.Models.HistorialVisitas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.HoraDeEntrada)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoraDeSalida)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VisitanteId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoraDeEntrada)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HoraDeSalida)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VisitanteId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HistorialId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HistorialId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HistorialId })
        </td>
    </tr>
}

</table>
